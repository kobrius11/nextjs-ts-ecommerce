// import { NextRequest } from "next/server";
// import { connectToDb } from "@/lib/db";

// export default async function GET(request: NextRequest ) {
//     const users = (await connectToDb()).db.collection('users').find({});

    
//     return Response.json(, {
//         status: 200
//     })
// }